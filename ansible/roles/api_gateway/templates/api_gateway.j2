[Unit]
Description=Krakend API Gateway

[Service]
TimeoutStartSec=60
Restart=always
ExecStart=/usr/bin/docker run -p 8080:8080 -v $PWD:/etc/krakend/ --rm --name %n {{ image_name }} --config /etc/krakend/krakend.json
ExecStop=/usr/bin/docker stop %n

[Install]
WantedBy=default.target

